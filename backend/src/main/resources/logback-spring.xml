<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <property name="LOG_DIR" value="logs"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>

    <!-- Console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Full application log (rolling daily, keep 7 days) -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/job-crawler.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/job-crawler.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <totalSizeCap>200MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Crawler-only log — удобно читать после /crawler/run -->
    <appender name="CRAWLER_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/crawler.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/crawler.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <totalSizeCap>50MB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Crawler packages → crawler.log + job-crawler.log -->
    <logger name="com.jobcrawler.scheduler" level="DEBUG" additivity="true">
        <appender-ref ref="CRAWLER_FILE"/>
    </logger>
    <logger name="com.jobcrawler.processing" level="DEBUG" additivity="true">
        <appender-ref ref="CRAWLER_FILE"/>
    </logger>
    <logger name="com.jobcrawler.adapter" level="DEBUG" additivity="true">
        <appender-ref ref="CRAWLER_FILE"/>
    </logger>

    <!-- Noise reduction -->
    <logger name="org.springframework.data.redis" level="WARN"/>
    <logger name="org.hibernate.SQL" level="WARN"/>
    <logger name="com.microsoft.playwright" level="WARN"/>

    <!-- Root -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

</configuration>